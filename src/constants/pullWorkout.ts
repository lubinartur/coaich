import { WORKOUT_PROGRAM_TEMPLATES } from '@/constants/workoutPrograms';

/** Default Pull template (same as Quick Program → Pull). */
export const PULL_WORKOUT_EXERCISES = WORKOUT_PROGRAM_TEMPLATES.pull;
